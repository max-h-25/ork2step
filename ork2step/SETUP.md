# First-time setup (run once)

Open **Terminal** and paste this entire block, then press Enter:

```bash
cd ~/Downloads/ork2step && xattr -rd com.apple.quarantine . && chmod +x install.command start.command stop.command && ./install.command
```

That's it. From then on, use the **ork2step icon on your Desktop** to launch the app.
